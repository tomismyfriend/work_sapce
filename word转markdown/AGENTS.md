# AGENTS.md

## Project

Word (.docx) to Markdown converter. Currently contains only a sample input file (`calltrace_word.docx`); no source code or tooling exists yet.

## Status

- No build system, dependencies, tests, or lint config
- No language or framework chosen yet
- When implementing, pick a docx-parsing library appropriate for the target language (e.g. `python-docx`, `mammoth.js`, `pandoc`)
